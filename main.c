#include <stdio.h>
#include <stdlib.h>

#include "meinschmarren.h"

int main()
{
    int x, y, z;
    x = 5;
    y = 4;
    z = spezialFunktion(x, y);

    copyRight(2019);
    printf("Ergebnis: %d\n", z);

    gehalt = 2000;
    printReichtum(500);

    return 0;
}
